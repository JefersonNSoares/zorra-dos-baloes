import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
  },
  button: {
    backgroundColor: "#000",
    color: "#000",
  },
  backBallon: {
    backgroundColor: "#000",
  },
});
