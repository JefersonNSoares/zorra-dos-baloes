import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    container: {
        paddingHorizontal: 30,
    },
    button: {
        alignItems: "center",
        padding: 30,
        backgroundColor: '#61459B',
        borderRadius: 50
    },
    text: {
        color: "#fff",
        fontSize: 20,
        fontWeight: "bold",
        borderRadius: 50
    }
})